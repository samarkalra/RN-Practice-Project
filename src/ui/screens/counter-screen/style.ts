import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  buttonContainer: {
    width: 280,
    flexDirection: 'row',
    marginBottom: 8,
  },
  button: {
    flex: 1,
    borderWidth: 1,
    justifyContent: 'center',
    alignItems: 'center',
    padding: 8,
    borderRadius: 4,
  },
  buttonLeft: {
    marginRight: 8,
  },
});
